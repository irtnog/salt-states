clamav:
  packages: []
  services: []
  dbdir: /var/lib/clamav
  clamd_conf: /etc/clamav/clamd.conf
  freshclam_conf: /etc/clamav/freshclam.conf

  clamd:
    AllowSupplementaryGroup: yes

  freshclam:
    AllowSupplementaryGroup: yes
